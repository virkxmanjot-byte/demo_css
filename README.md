Download or clone this project to your local system.
Open the project folder and locate the index.html file.
Double-click on index.html or right-click → Open with any web browser (Chrome, Edge, etc.).
Make sure all files (HTML, CSS, images) are in the same folder so the design loads properly.
